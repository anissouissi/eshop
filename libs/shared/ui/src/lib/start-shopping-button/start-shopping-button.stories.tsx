import type { Meta } from '@storybook/react';
import { StartShoppingButton } from './start-shopping-button';

const meta: Meta<typeof StartShoppingButton> = {
  component: StartShoppingButton,
  title: 'StartShoppingButton',
};
export default meta;

export const Primary = {
  args: {},
};
