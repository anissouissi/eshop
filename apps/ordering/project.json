{
  "name": "apps/ordering",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/ordering/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/ordering",
        "main": "apps/ordering/src/main.ts",
        "tsConfig": "apps/ordering/tsconfig.app.json",
        "assets": ["apps/ordering/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/ordering/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apps/ordering:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "apps/ordering:build:development"
        },
        "production": {
          "buildTarget": "apps/ordering:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/ordering/jest.config.ts"
      }
    }
  },
  "tags": []
}
