{
  "name": "apps/account",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/account/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/account",
        "main": "apps/account/src/main.ts",
        "tsConfig": "apps/account/tsconfig.app.json",
        "assets": ["apps/account/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/account/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apps/account:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "apps/account:build:development"
        },
        "production": {
          "buildTarget": "apps/account:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/account/jest.config.ts"
      }
    }
  },
  "tags": []
}
